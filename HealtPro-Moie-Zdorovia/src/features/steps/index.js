// Steps feature module.
// The step counter logic (toggleStepCounter, enableSteps, handleMotion,
// updateStepUI) currently lives in src/app.js to share state with the
// dashboard. This module is the designated location for that logic;
// future refactors should extract it here.
export {};
